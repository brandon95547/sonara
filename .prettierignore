node_modules
dist
coverage
package-lock.json

# A verbatim copy of the UI Bible's token layer. Formatting it here makes the
# next re-copy a diff full of whitespace, which is how a "verbatim" file quietly
# stops being one.
apps/web/src/styles/tokens.css
